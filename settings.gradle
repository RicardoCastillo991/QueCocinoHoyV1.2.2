pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    rootProject.name = "QueCocinoHoy"
    include ':app'
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

    }

}